server:
  port: 9094
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka


  application:
    name: ORDER-SERVICE

  data:
    mongodb:
      uri: mongodb://localhost:27017/orderDb
      host: localhost
      port: 27017
      database: orderDb
      repositories:
        enabled: true
